package ar.edu.unlp.info.oo1.ejercicio6;
import java.util.ArrayList;
import java.util.List;

public class Farola {
	private boolean estado;
	private List<Farola> farolas;
	
	public Farola(){
		this.estado=false;
		farolas= new ArrayList<>();
	}
	
	private void addNeighbor(Farola otraFarola){
		this.farolas.add(otraFarola);
	}
	
	public void pairWithNeighbor(Farola otraFarola){
		this.farolas.add(otraFarola);
		otraFarola.addNeighbor(this);
	}
	
/*	public List<Farola> getNeighbors(){
		List<Farola> farolasCopia = new ArrayList<>();
		for(Farola farola:farolas){
			farolasCopia.add(farola);
		}
		return farolasCopia;
	}	
	
	*/
	
	//Collections.copy(farolasCopia,this.farolas); ???
	//Mal pero use para test 
	public List<Farola> getNeighbors(){
		List<Farola> farolasCopia = new ArrayList<Farola>(this.farolas);
		return farolasCopia;
	}
	
	public boolean isOn(){
		return estado;
	}
	
	public void turnOn(){
		this.turn(true);
	}
	
	public void turnOff(){
		this.turn(false);
	}
	
	private void setEstado(boolean estado){
		this.estado=estado;
	}
	
	private void changeState(boolean nuevoEstado, Farola farola){
		if(nuevoEstado != farola.isOn()){
			farola.setEstado(nuevoEstado);
		}
	}
	
	private void turn(boolean nuevoEstado){
		if(nuevoEstado != this.isOn()){
			this.farolas.stream().forEach(farola -> farola.changeState(nuevoEstado, farola));
		}
	}
	
	
}
